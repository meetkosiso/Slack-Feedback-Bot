const newFeedback = require('./create-feedback');
const feedbackCreated = require('./feedback-created');
const feedbackCreationError = require('./feedback-creation-error');

module.exports = {
	newFeedback,
	feedbackCreated,
	feedbackCreationError,
};
